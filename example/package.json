{
  "name": "example-ahoy",
  "version": "0.1.0",
  "description": "",
  "main": "main.bundle.js",
  "scripts": {
    "dev": "electron main.js",
    "start": "electron main.bundle.js",
    "build": "npm run build:main && npm run build:ui",
    "build:main": "noderify main.js --filter=electron > main.bundle.js",
    "build:ui": "echo 'building ui...'",
    "pack": "electron-builder --config builder/config.js",
    "release": "npm run build && npm run pack",
    "lint": "standard --fix"
  },
  "author": "",
  "license": "AGPL-3.0",
  "dependencies": {
    "ssb-ahoy": "^5.0.0",
    "ssb-backlinks": "^2.1.1",
    "ssb-db": "^20.4.1"
  },
  "devDependencies": {
    "noderify": "^5.1.0",
    "standard": "^16.0.4"
  }
}
